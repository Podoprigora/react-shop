import React from "react";
import PropTypes from "prop-types";

const LinearProgress = params => (
  <div className="linear-progress">
    <div className="linear-progress__bar" />
  </div>
);

export default LinearProgress;
